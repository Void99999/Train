{
  "name": "last-train",
  "version": "0.1.0",
  "private": true,
  "description": "LAST TRAIN - first-person survival train game.",
  "type": "module",
  "scripts": {
    "start": "node tools/serve.js",
    "test": "node --test \"tests/*.test.js\"",
    "check": "node tools/check-locales.js && node --test \"tests/*.test.js\"",
    "build": "node tools/build-standalone.js"
  },
  "engines": {
    "node": ">=20"
  }
}
